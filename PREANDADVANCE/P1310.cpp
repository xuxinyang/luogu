#include <bits/stdc++.h>
using namespace std;
const int maxl = 1e5+5;
const int mod = 1e4+7;
char st[maxl], a[maxl], ans[maxl];
int n, top, idx;
int main()
{
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i];
    
    return 0;
}