#include <bits/stdc++.h>
using namespace std;
#define ll long long
const int maxn = 45;
ll n, m, a[maxn], f[maxn];
void dfs(int idx, int cur, ll sum)
{

}
int main()
{
    cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> a[i];
    sort(a+1, a+n+1);
    for (int i = 1; i <= n; i++)
    {

    }
    return 0;
}